# Recommended Python version: 3.8 - 3.11
# Core dependencies used by the app
streamlit>=1.20.0,<2.0
pandas>=1.5.3
numpy>=1.24.0
matplotlib>=3.6.2
plotly>=5.15.0

# Optional / extras (used if you want wordcloud visuals)
wordcloud>=1.8.2
Pillow>=9.4.0

# Lock file note:
# If you need exact reproducible environments, run `pip freeze > requirements.txt`
# and commit that output instead of these loose ranges.
