# INTEND_DASHBOARD
# Streamlit App with Mediapipe

This app uses Streamlit and Mediapipe to perform real-time hand tracking and visualization.

## 🚀 Features
- Upload image and detect hand landmarks
- Visualize results using OpenCV
- Interactive UI with Streamlit

## 📦 Requirements
All dependencies are listed in `requirements.txt`. Python 3.10 is recommended.

## 🛠️ Setup

```bash
pip install -r requirements.txt
streamlit run app.py
